import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.acordespro.app',
  appName: 'AcordesPro',
  webDir: 'dist',
  android: {
    backgroundColor: '#FFFFFFFF',
  },
};

export default config;
